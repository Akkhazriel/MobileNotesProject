import React from 'react';
import { View, Text } from 'react-native';

const NotesScreen = () => {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-xl text-black">Notes</Text>
    </View>
  );
};

export default NotesScreen;
